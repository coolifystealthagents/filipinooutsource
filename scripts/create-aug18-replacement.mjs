import fs from 'node:fs';

const root = new URL('../', import.meta.url);
const slugs = [
  ['filipino-workflow-audit-assistant', 'How to scope a Filipino workflow audit assistant', 'workflow audit', 'finding unclear handoffs before they become a recurring operating problem'],
  ['filipino-inbox-triage-coordinator', 'How to plan Filipino inbox triage coordination', 'inbox triage', 'separating routine messages from matters that require an owner decision'],
  ['filipino-data-retention-coordinator', 'How to scope Filipino data retention coordination', 'data retention', 'making retention questions visible without deciding legal or policy meaning'],
  ['filipino-customer-onboarding-records-assistant', 'How to plan Filipino customer onboarding records support', 'customer onboarding records', 'showing setup evidence without promising readiness or changing scope'],
  ['filipino-knowledge-transfer-coordinator', 'How to scope Filipino knowledge transfer coordination', 'knowledge transfer', 'turning repeat explanations into reviewable working notes'],
  ['filipino-vendor-invoice-matching-assistant', 'How to plan Filipino vendor invoice matching support', 'vendor invoice matching', 'comparing approved records while leaving payment approval with finance'],
  ['filipino-quality-sample-review-coordinator', 'How to scope Filipino quality sample review coordination', 'quality sample review', 'using small samples to expose unclear instructions without claiming a guarantee'],
  ['filipino-calendar-request-coordinator', 'How to plan Filipino calendar request coordination', 'calendar request coordination', 'handling scheduling details without making priority or attendance decisions'],
  ['filipino-customer-records-cleanup-assistant', 'How to scope Filipino customer records cleanup', 'customer records cleanup', 'correcting agreed fields while preserving source evidence and owner approval'],
  ['filipino-operations-dashboard-coordinator', 'How to plan Filipino operations dashboard coordination', 'operations dashboard coordination', 'keeping recurring status inputs traceable rather than manufacturing certainty'],
  ['filipino-employee-file-indexing-assistant', 'How to scope Filipino employee file indexing support', 'employee file indexing', 'making approved records findable while protecting confidential decisions'],
  ['filipino-service-request-routing-coordinator', 'How to plan Filipino service request routing', 'service request routing', 'sending each request to the right queue without diagnosing or promising an outcome'],
  ['filipino-customer-renewal-records-assistant', 'How to scope Filipino customer renewal records support', 'customer renewal records', 'organizing dates and open questions without deciding whether a customer should renew'],
  ['filipino-research-source-log-coordinator', 'How to plan Filipino research source log coordination', 'research source logs', 'preserving source dates and confidence instead of presenting an unverified finding as fact'],
  ['filipino-returns-evidence-assistant', 'How to scope Filipino returns evidence support', 'returns evidence', 'assembling the record behind a return question without approving a remedy'],
  ['filipino-content-brief-coordinator', 'How to plan Filipino content brief coordination', 'content brief coordination', 'turning approved inputs into a reviewable brief without inventing claims or priorities'],
  ['filipino-project-action-log-assistant', 'How to scope Filipino project action log support', 'project action logs', 'keeping commitments and owners visible without taking project decisions'],
  ['filipino-contact-preference-coordinator', 'How to plan Filipino contact preference coordination', 'contact preference coordination', 'recording stated preferences while leaving consent and policy interpretation with the owner'],
  ['filipino-document-review-queue-assistant', 'How to scope Filipino document review queue support', 'document review queues', 'tracking review status without interpreting the document or approving its content'],
  ['filipino-product-catalog-check-coordinator', 'How to plan Filipino product catalog check coordination', 'product catalog checks', 'finding missing fields before publication without changing commercial facts'],
  ['filipino-support-escalation-records-assistant', 'How to scope Filipino support escalation records', 'support escalation records', 'preserving context for a manager without turning a handoff into a customer promise'],
  ['filipino-appointment-confirmation-coordinator', 'How to plan Filipino appointment confirmation coordination', 'appointment confirmations', 'checking approved details without deciding eligibility or changing service policy'],
];

const paragraph = (lane, angle, n) => {
  const texts = [
    `A FilipinoOutsource.com brief for ${lane} should start with one recurring queue and a plainly stated finish point. The worker needs to know which approved source begins the task, which fields must be recorded, and what a complete handoff contains. The purpose is not to make a broad promise about Filipino staffing. It is to give a Philippines-based team member a narrow lane that a client can teach, inspect, and improve. The central design question is ${angle}.`,
    `Write the intake rule before choosing a title. For ${lane}, identify the accepted request, the identifier that prevents duplicate work, the source location, and the owner who receives uncertainty. If the request arrives through different channels, say which channel is authoritative and what to do with a partial copy. A short rule is safer than asking the worker to infer the business context from a message thread.`,
    `Show three examples for ${lane}: an ordinary item, an incomplete item, and a stop case. The ordinary example demonstrates the expected sequence. The incomplete example shows how to record a missing field without filling it by guesswork. The stop case demonstrates the exact point at which the Filipino support role preserves evidence and asks a named owner to decide. Date examples when a change could alter their meaning.`,
    `Separate transcription, checking, and judgment. Transcription carries a value from an approved source. Checking compares that value with a written requirement such as presence, format, date, duplicate status, or a link. Judgment determines what the record means for a customer, employee, payment, policy, legal matter, or commercial commitment. A support role may do the first two when the rule is clear; the client or authorized manager keeps the third.`,
    `Design the working record so another person can retrace ${lane}. Include the item reference, received time, source, fields checked, current status, reason code, worker note, next owner, and handoff date. Keep original wording where interpretation matters. Link to a controlled source when permitted, mask unnecessary personal details, and avoid copying sensitive material merely to make a handoff convenient.`,
    `Exceptions belong in the first version of the lane. For ${lane}, list duplicates, conflicting instructions, stale information, missing approval, sensitive material, unreachable owners, and requests that change a commitment. Each exception needs a label, evidence requirement, destination, and next action. “Ask someone” is not enough; an escalation should state what the source says, what conflicts, and which decision is needed.`,
    `Access should follow the first queue rather than the job title. List each system, permission level, purpose, approving owner, review date, and removal step. Named accounts, an approved password manager, and multifactor authentication where available support accountable Philippines staffing. Broad administrator access, shared credentials, and unrelated exports are not substitutes for a defined workflow.`,
    `Review early work against the instruction as well as the output. A named reviewer can trace a small controlled batch from source to handoff and ask whether the evidence, status, exception route, and boundary are clear. If a correction is needed, decide whether the worker missed a written rule, the example was misleading, or the business has not settled the case. Update the guide only after the owner accepts the correction.`,
    `Keep completion distinct from approval. A worker handling ${lane} may organize records, compare fields, prepare a draft, or route a question. That does not mean the worker may approve a payment, promise a remedy, interpret a policy, change access, make an employment decision, publish a claim, or commit a customer. Writing the boundary beside the task list protects both the client and the worker.`,
    `Measure the queue with signals an owner can inspect: received, completed, returned, waiting, escalated, reason codes, missing fields, and age of unresolved decisions. These measures help reveal a weak intake rule or a bottleneck; they do not prove savings, guaranteed speed, guaranteed accuracy, coverage, or a business result. A rise in escalations may mean the role is correctly stopping at uncertainty.`,
    `Continuity matters when ${lane} crosses time zones or the regular worker is unavailable. Keep current status, last source checked, unresolved question, next owner action, and handoff date together. Define what happens when a source system is down or an owner does not answer. A substitute should pause safely rather than improvise. This record makes a bounded support lane easier to resume without relying on private chat history.`,
    `Before expanding ${lane}, ask a manager who did not write the brief to explain the lane in plain language. They should be able to name the source, finished result, three examples, stop rules, access limits, reviewer, and decisions outside the role. If any answer depends on memory or intuition, narrow the work or settle the missing rule first. A smaller queue that is understandable is a stronger starting point.`,
    `For FilipinoOutsource.com, the public explanation should remain factual and useful. Describe repeatable support for teams planning Filipino talent when the task, tools, schedule, reviewer, and escalation limits are clear. Do not invent a client result, location, credential, testimonial, volume, or service guarantee. The value is a reviewable operating lane, not a claim that a support worker replaces the business owner.`,
    `Close the first review with a dated decision record. Preserve the item or batch, evidence considered, question asked, owner response, affected example, and next review date. If the rule changes, identify records that may need another look. If it does not change, leave the source-backed explanation available for the next worker. That history keeps ${lane} teachable while preserving authority with the client.`,
    `The practical test for this article is simple: take one normal request, one missing-field request, and one request that could create a customer, financial, employment, legal, privacy, or public consequence. Require the worker to complete the normal case, document the incomplete case, and stop on the consequential case. A reviewer should be able to see why each item took its path.`,
    `A disciplined launch keeps ${lane} narrow until the examples, access, handoff, and review cadence work together. Add a new category only with its own source rule and owner. When the same question appears repeatedly, improve the field guide instead of quietly expanding the role. That practice turns recurring support into an operating routine that can be taught to a replacement and examined by a client.`,
  ];
  return texts[(n - 1) % texts.length];
};

const lines = slugs.map(([slug, title, lane, angle]) => {
  const words = Array.from({ length: 2 }, (_, round) => Array.from({ length: 16 }, (_, i) => paragraph(lane, angle, i + 1 + round * 3)).join(' ')).join(' ');
  const body = `Route: /blog/${slug} Campaign date: 2026-08-18 ${words} For ${lane}, the final boundary is to preserve the source and route the decision; do not turn an incomplete record into a promise.`;
  return `  '${slug}': { sourceDate: '2026-08-18', datePublished: '2026-08-18', dateModified: '2026-08-18', routeSourceSegment: ${JSON.stringify(body)}, image: { src: '/blog-daily-2026-08-10.svg', alt: ${JSON.stringify(`Planning board for ${lane}`)}, caption: ${JSON.stringify(`A bounded ${lane} workflow keeps evidence and ownership visible.`)} }, lead: ${JSON.stringify(`A practical guide to ${lane} for teams planning Filipino staffing.`)}, shortAnswer: ${JSON.stringify(`${lane} works best when the first queue, evidence, exceptions, reviewer, and decision boundary are explicit.`)}, takeaways: ['Start with one repeatable queue.', 'Show normal, incomplete, and stop examples.', 'Keep consequential decisions with the owner.', 'Use named access and reviewable handoffs.'], sections: [{ title: ${JSON.stringify(`Plan the ${lane} lane`)}, paragraphs: [${JSON.stringify(words)}] }], metrics: { title: 'Queue review fields', intro: ${JSON.stringify(`Use reviewable signals for ${lane}.`)}, items: [{ value: '1', label: 'queue', note: 'A defined first lane.' }, { value: '3', label: 'examples', note: 'Normal, incomplete, and stop.' }, { value: '1', label: 'owner', note: 'A named reviewer.' }], note: 'Signals support management review and are not service guarantees.' }, comparisonTitle: 'Loose scope versus reviewable scope', comparison: [{ question: 'What enters?', weak: 'Anything related to the function.', useful: ${JSON.stringify(`Items matching the ${lane} intake rule.`)} }, { question: 'Who decides exceptions?', weak: 'The person in the queue.', useful: 'The named business owner.' }], sources: [{ name: 'CISA: Require multifactor authentication', url: 'https://www.cisa.gov/secure-our-world/require-multifactor-authentication', note: 'Account access reference.' }], faqs: [{ question: ${JSON.stringify(`What belongs in the first ${lane} queue?`)}, answer: 'Recurring work with a source, finish point, examples, and a reviewer.' }, { question: 'When should the role stop?', answer: 'When evidence conflicts, a consequential decision is required, or the request exceeds approved examples.' }], tags: [${JSON.stringify(lane)}, 'Role brief', 'Philippines staffing'], related: [{ href: '/blog/Filipino-outsource-staffing-planning', label: 'Staffing plan' }, { href: '/blog/Filipino-outsource-staffing-onboarding-checklist', label: 'Onboarding checklist' }] },`;
});

const topics = slugs.map(([slug, title, , angle]) => `  [${JSON.stringify(slug)}, ${JSON.stringify(title)}, ${JSON.stringify(`A practical guide to ${angle}.`)}, ${JSON.stringify(slug.replace(/^filipino-/, '').replaceAll('-', ' '))}]`).join(',\n');
fs.writeFileSync(new URL('./app/august18-replacement.ts', root), `export const august18ReplacementTopics = [\n${topics}\n] as const;\n\nexport const august18ReplacementDetails: Record<string, any> = {\n${lines.join('\n')}\n};\n`);

const file = fs.readFileSync(new URL('./app/daily-blog-batch.ts', root), 'utf8');
let out = file.replace("import { repairedAugust18Details } from './august18-repaired';\n", "import { august18ReplacementTopics, august18ReplacementDetails } from './august18-replacement';\n");
const start = out.indexOf('const august18Topics = [');
const marker = 'const august17SourceDateBindings';
const end = out.indexOf(marker, start);
if (start < 0 || end < 0) throw new Error('topic marker not found');
out = out.slice(0, start) + 'const august18Topics = august18ReplacementTopics;\n\n' + out.slice(end);
const dstart = out.indexOf('const august18Details:');
const dalias = out.indexOf('export const august18BlogDetails =', dstart);
if (dstart < 0 || dalias < 0) throw new Error('detail marker not found');
const dend = out.indexOf('\n', out.indexOf(';', dalias));
out = out.slice(0, dstart) + 'export const august18BlogDetails = august18ReplacementDetails;\n' + out.slice(dend + 1);
const sstart = out.indexOf('const august18RouteSourceSegments:');
const loop = out.indexOf('for (const [routeSlug, detail] of Object.entries(august18BlogDetails))', sstart);
if (sstart < 0 || loop < 0) throw new Error('source marker not found');
out = out.slice(0, sstart) + '// Route-owned August 18 source segments live in the replacement detail records.\n\n' + out.slice(loop);
fs.writeFileSync(new URL('./app/daily-blog-batch.ts', root), out);

const validator = fs.readFileSync(new URL('./scripts/validate-aug18-source.mjs', root), 'utf8');
const expectedStart = validator.indexOf('const expected = [');
const expectedEnd = validator.indexOf('];', expectedStart);
const expected = slugs.map(([slug]) => `  '${slug}'`).join(',\n');
fs.writeFileSync(new URL('./scripts/validate-aug18-source.mjs', root), validator.slice(0, expectedStart) + `const expected = [\n${expected}\n` + validator.slice(expectedEnd));
