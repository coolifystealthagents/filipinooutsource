import {notFound} from 'next/navigation'; import {Header,Footer,CTA,JsonLd} from '../../components'; import {fleetServices} from '../../fleet-data'; import {site} from '../../data';
const base=`https://${site.domain.toLowerCase()}`; export function generateStaticParams(){return fleetServices.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=fleetServices.find(x=>x.slug===slug);if(!s)return {title:'Not found'};return {title:s.title,description:s.desc,alternates:{canonical:`/services/${slug}`}}}
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=fleetServices.find(x=>x.slug===slug);if(!s)notFound();const url=`${base}/services/${s.slug}`;const role=s.title.toLowerCase();const faqs=[['What work should this role start with?',`Start ${role} with repeatable work that has examples, a clear finish point, and a client-side reviewer.`],['What should stay with my team?',`Keep final decisions, sensitive approvals, access changes, and unusual exceptions with a named manager while the Filipino team member prepares the work.`],['How do we keep the role controlled?',`Use named accounts, written limits, small batches, and review notes that can be added back into the working instructions.`]];return <><Header/><main><JsonLd data={{'@context':'https://schema.org','@type':'Service',name:s.title,description:s.desc,url,areaServed:'Worldwide',provider:{'@type':'Organization',name:site.brand}}}/><section className="fleet-hero variant-0"><div className="container"><p className="eyebrow">Philippines-based business operations support</p><h1>{s.title}</h1><p className="lead">{s.desc}</p><a className="btn primary" href="/contact">Discuss this service</a></div></section><section className="section service-depth"><div className="container"><div className="service-intro-grid"><article className="card"><p className="eyebrow">What this role is for</p><h2>Give {role} a narrow lane first.</h2><p>This page is a role brief, not a vague service label. Use it to decide the first queue, the examples a candidate should review, and the approval points that stay with your team.</p><p>A Filipino team member can be effective when the work is repeatable and the manager can inspect early output. The clearer the queue, the easier it is to compare candidates and improve the handoff.</p></article><article className="card"><p className="eyebrow">Best starting point</p><h2>Define the first handoff.</h2><p>Choose one workflow that repeats often enough to matter. Then collect sample items, note common exceptions, and decide who will answer questions during launch.</p><p>Do not bundle unrelated work just because one person may be capable of helping. Add work only after the first queue is accurate and easy to review.</p></article></div><div className="fleet-detail-grid service-detail-grid"><div className="card"><h2>Work to include</h2><ul>{s.tasks.map(x=><li key={x}>{x}</li>)}</ul></div><div className="card"><h2>Controls to retain</h2><ul>{s.controls.map(x=><li key={x}>{x}</li>)}</ul></div><div className="card"><h2>Launch plan</h2><ul>{s.firstWeek.map(x=><li key={x}>{x}</li>)}</ul></div></div><section className="service-checklist card"><h2>Readiness checklist</h2><div><span>Examples are ready</span><span>Access is limited</span><span>Reviewer is named</span><span>Stop rules are written</span></div><p>If one of these is missing, tighten the scope before asking for a shortlist. A better role brief protects the business and gives the Filipino team member a fair standard.</p></section><section className="faq-stack service-faq"><h2>Questions buyers ask</h2>{faqs.map(([q,a])=><article key={q}><h3>Q: {q}</h3><p>A: {a}</p></article>)}</section></div></section><CTA/></main><Footer/></>}
